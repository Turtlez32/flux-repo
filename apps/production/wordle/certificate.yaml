apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wordle-tls
  namespace: wordle
spec:
  secretName: wordle-tls
  dnsNames:
    - wordle.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
