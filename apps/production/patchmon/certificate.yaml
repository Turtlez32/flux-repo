apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: patchmon-tls
  namespace: patchmon
spec:
  secretName: patchmon-tls
  dnsNames:
    - patchmon.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
