apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: patchmon
spec:
  selector:
    app: patchmon-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
