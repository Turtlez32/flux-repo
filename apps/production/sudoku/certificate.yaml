apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sudoku-tls
  namespace: sudoku
spec:
  secretName: sudoku-tls
  dnsNames:
    - sudoku.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
