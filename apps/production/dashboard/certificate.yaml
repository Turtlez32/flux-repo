apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-tls
  namespace: dashboard
spec:
  secretName: dashboard-tls
  dnsNames:
    - dashboard.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
