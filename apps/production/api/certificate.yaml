apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-tls
  namespace: api
spec:
  secretName: api-tls
  dnsNames:
    - api.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
