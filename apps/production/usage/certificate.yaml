apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: usage-tls
  namespace: usage
spec:
  secretName: usage-tls
  dnsNames:
    - usage.turtleware.au
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
